import 'package:flutter/widgets.dart';

class LiveInfoPage extends StatelessWidget {
  const LiveInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
